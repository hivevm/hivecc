/**
 * Token literal values and constants.
 * Generated by org.javacc.parser.OtherFilesGen#start()
 */
public interface {{PARSER_NAME}}Constants {

  // End of File
  int EOF = 0;

  // RegularExpression Ids
@FOREACH(TOKENS, ordinal, label)
  int {{label}} = {{ordinal}};
@end

  // Lexical states
@FOREACH(STATES, index, state)
  int {{state}} = {{index}};
@end

  // Literal token values
  String[] tokenImage = {
    "<EOF>",
@FOREACH(PRODUCTIONS, regexp)
    {{regexp}}
@END
  };
}